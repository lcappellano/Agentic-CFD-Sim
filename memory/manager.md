# Manager notes (reusable lessons only; project narrative lives in each run's report.md and docs/history/)

- Environment: WSL2 Ubuntu, Python 3.14 in `.venv` (created `--without-pip`; pip installed later), OpenCFD OpenFOAM v2412 patch 260127 at `/usr/lib/openfoam/openfoam2412`, Gmsh 4.14.0 runtime with the vendored wrapper. `tools/workbench.py doctor` confirms. The pipeline sources OpenFOAM itself.
- Browser checks need `PLAYWRIGHT_BROWSERS_PATH=/tmp/cooling-browser-binaries` and `LD_LIBRARY_PATH=/tmp/cooling-browser-libs/extracted/usr/lib/x86_64-linux-gnu` (Chromium 1208 already extracted there).
- Users give flow in L/min and pressure in bar; the spec accepts those units directly. Ask whether a pressure is absolute, gauge, or a pump rise before writing the spec; do not guess. An outlet pressure is not a pump rise.
- Three of flow, outlet pressure and pump rise over-determine a case. Impose flow and outlet pressure; report the pressure rise against the target.
- The first real part (Astra manifold, September 2026) reached 111.6 °C heated maximum at 40 L/min and 8 bar absolute outlet on the tet mesh; pressure drift and residual gates failed, so it stays `diagnostic`. Its handoff is `runs/20260918T002459-astra-manifold-review-27f127f8/handoffs/revision-6-4824154e` and `specs/example-manifold-40lpm.json` reproduces the setup.
- Approvals: the requirements viewer is the only place a user approves; agents never call approve on a real case. Synthetic approvals live only in tests.
- Report-replication sweep on the Astra manifold (2026-09-19, runs `*-manifold-copper-{40,30}lpm-8bar`, `*-24lpm-{8,15}bar`; copper, 20 °C in, 10 MW/m²; tet-medium + 0.2 mm channel box, 2.86 M cells, SST Spalding, 8 ranks ≈ 2.3 s/iteration, 2.1 h per 3000-iteration point): heated max 154.4 / 178.0 / 198.0 °C at 40 / 30 / 24 L/min against the SU2 single-channel report's 168 / 182 / 205 °C (CuCrZr; copper ≈ 9 K lower), outlet and channel flow sharing within 2 K and 3 %, but pressure drop 2.83 bar vs the report's 4.97 bar at 40 L/min. Open item: channel friction on this mesh (5 cells across the 1 mm gap, wall functions) — a finer channel mesh at 40 L/min is the pending check. At 24 L/min and 8 bar the wetted wall (173 °C) exceeds saturation and the runner stops at 600 iterations by design; 15 bar outlet gives the same temperatures and converges (constant properties).
- 40 L/min residual floor (2026-09-21): the 3000-iteration copper sweep points are settled in temperature and energy but sit on a fluid residual floor that is flat from iteration 250 and identical at all three flows; the pressure drop (2.83 bar) is 1.5 channel dynamic heads for the whole part against a hand estimate of about 4.8 bar per channel. Fixes now in code: `initialization.fields_from_case` + profile `tet-robust-restart` (restart with the unlimited solid gradient from a settled case), gate-aware next-step advice in the report, `cucrzr` in the material library. Refined-mesh rerun 2026-09-21 (run 20260921T201334, `specs/manifold-copper-40lpm-fine.json`, 0.15 mm HXT box over channels and header slots, 7.6 M cells, started from the coarse solution via `fields_mapped_from_case`): `diagnostic`, stopped at 600 iterations by the liquid screen on one cell at the outermost channel's entrance edge (-2.0 bar absolute); 2.67 bar and 151.4 C against 2.83 bar and 154.4 C on the 0.2 mm mesh, so refinement does not close the gap to the report's 4.97 bar. Open for Luca: treat the edge minimum as a cavitation flag (raise outlet pressure), as a numerical edge singularity (tolerance in the phase screen, a code change), or round the entrance edges (geometry change). Two earlier attempts in the same run directory failed (sliver mesh at 0.12 mm serial; potential-flow start divergence) and led to the mesh quality gate and the mapped start; a slow-start profile was tried and removed.
- Design variants (2026-09-22): Luca asked for a modified STEP; `src/cad/variants.py` produced `inputs/astra-manifold-variants/Astra_Manifold_bullnose.STEP` and `Astra_Manifold_bullnose-vanes2.STEP` with provenance JSONs and plan-section SVGs; review-import runs `20260922T002302-astra-manifold-bullnose-88afa256` and `20260922T002305-astra-manifold-bullnose-vanes2-98af1d06` are waiting for Luca's port and heated-face selection and approval before any spec can run. The original CAD was not touched.
- Revision B (Luca's own flow redesign, `inputs/Cooling_Test_Part_Flow_Revision_B.STEP`, run `20260922T003000-manifold-revB-40lpm-8bar`, launched outside the agent session 2026-09-22): 5.6 M cells, 3000 iterations in 5.0 h, `diagnostic` on the usual residual floor with temperatures settled; heated 151.7 C, wetted 130.9 C, dp 2.59 bar, min absolute pressure 2.57 bar, no phase violations; channel-share spread 11 % (original 21 %), outermost/central 0.935 (0.873); entrance loss gone and inlet expansion loss 0.21 bar (0.32), but exit loss 1.05 bar (0.66) with a 48 m/s peak at the channel exits and a 78 m/s spike in the inlet slot. Report advice now points to the restart profile and a finer mesh. Next for Luca: restart (1.5 h) or look at the exit edges.
- Independent benchmark 2026-09-22: Luca asked for a publicly known result and waived the CAD review for a generated part; the smooth round tube (Petukhov / Gnielinski, experimental fits) passes at +7.6 % friction and -5.8 % Nusselt on the manifold-style mesh. The manifold channels themselves sit 30 % low on in-channel friction with the same turbulence level, so the SU2-report gap is channel-specific (section, corner fillets, or the turned feed), not a generic wall-function error. Next benchmark: a straight 1 x 2 mm filleted channel at 19.6 m/s. Benchmark parts live in `inputs/benchmarks/`, run unapproved by design; docs/pipeline.md has the workflow.
- Revision B manifold (0.1 mm channel fillets, 0.5 mm bullnose rib ends; run `runs/20260922T003000-manifold-revB-40lpm-8bar`, 5.6 M cells with 0.12 mm rib-end boxes, 5 h for 3000 iterations at 6 s/it) vs revision A at 40 L/min, 8 bar, copper: heated 151.7 vs 154.4 °C, Δp 2.59 vs 2.83 bar, channel flow sharing flattened (outer channels 94-97 % of mean vs 87-92 %), minimum wetted pressure 2.57 vs 1.85 bar. The exit pocket persists at the channel ceiling edge (x = -26.1, y = 2.0 mm): the bullnoses round the ribs, not the floor/ceiling edges. Pressure drift (2.8 %) and p_rgh residual (1.4e-2) did not improve; a 78 m/s spike near the inlet-side rib ends and min SICN 0.04 point at mesh quality in the fine boxes, not geometry.
