# Legacy fixed-demo adapters

Frozen scripts from the first through-bore demonstration (September 2026).
They reproduce that one run's evidence and are **not imported by the live
pipeline**. Use `src/` for any new part. See `docs/history/baseline-demo.md`.
